# READYS — Contexto del proyecto

## Objetivo
Sitio web de NUTEC. Trabajo actual enfocado en diagnosticar errores persistentes en el home en español (`/es`).

## Estado actual
Diagnóstico de LCP confirmado y solución propuesta; pendiente aplicarla desde el Designer de Webflow (el usuario pregunta cómo hacerlo con el componente nativo Background Video).

## Cambios realizados
- [2026-09-14] FIX LCP MÓVIL APLICADO: Usuario cambió a `eager` las imágenes del slider de productos (`MaxWool 2300`, `SuperMag 2200`). Resultado inmediato en móvil: score subió de 42 a 58.
- [2026-09-14] Fila "texto visible durante carga de fuente" en https://www.nutec.com/ (page-seed.html): el culpable NO son las fuentes Webflow (siguen con swap) sino `cdn.icomoon.io/146409/cliengowidget/icomoon.woff2` del widget Cliengo (150ms+60ms). El @font-face sin swap vive en el CSS de terceros, no se corrige con CSS propio; fix = diferir Cliengo fuera de la carga crítica (ojo: Cliengo se inyecta por GTM, el fix va en el trigger del tag). [2026-09-14: HALLAZGO MÓVIL LCP — En pantallas móviles nutec.com, las imágenes del slider de productos del hero (`MaxWool 2300`, `SuperMag 2200`) se renderizan muy arriba. Tienen `loading="lazy"` por lo que Lighthouse las marca como LCP lazy-loaded. Fix: cambiar `loading="lazy"` a `loading="eager"` en Webflow para las 2-3 imágenes iniciales de ese carrusel.]
- [2026-08-25] FIX VIDEO HERO aplicado también en home EN https://www.nutec.com/ (replica del fix de /es): `<video preload="none" muted loop playsinline>` sin autoplay + arranque diferido startHeroVideo (window load → requestIdleCallback timeout 3s → play()) + fade-in .is-ready. VERIFICADO EN VIVO: 0 bytes de video descargados durante la carga completa (readyState:0, networkState:0), poster JPG 93KB carga solo como LCP, total crítico baja de ~10.9-15.5MB a ~2,557 KiB. Expectativa PSI: "Enormes cargas útiles" seguirá roja (la descarga entra en la ventana de traza de Lighthouse vía load+idle≤3s, igual que en /es), pero mejora LCP/TBT de usuarios reales. PENDIENTE: quitar source WebM (12.7MB, Chrome usa mp4 primero) + recomprimir mp4 a ~3MB. Nota: quedó un script viejo comentado de arranque-por-interacción en el HTML (inofensivo).
- [2026-08-25] Fila "Enormes cargas útiles" en https://www.nutec.com/ (home EN): SIGUE FALLANDO (limitación aceptada 08-24 por autoplay del cliente), ahora con números exactos. Base SIN video ya excede presupuesto: ~2,557 KiB vs 1,600 KiB (imágenes 1.4MB + JS 592KB [GTM proxy 500KB + jQuery 89KB] + fuentes 364KB + HTML/CSS 195KB). En PSI real el video entra completo: +8.4MB si carga mp4 o +12.7MB webm (ambas fuentes siguen publicadas) → ~10.9–15.5MB total. Con el fix acordado (solo mp4 recomprimido ~3MB) quedaría ~5.6MB (-58%) pero la fila seguirá roja. Hallazgo extra: las 3 .woff legacy "Untitled Sans" se descargan aún (~119KB) aunque NINGÚN elemento del home usa ya esa familia (0 elementos computados vs 401 con Opt) → borrarlas de Project Settings ahorra ~119KB. Nunito descarga 5 subsets (~137KB).
- [2026-08-25] Fila "Elimina CSS no utilizado" en https://www.nutec.com/: ANALIZADA A FONDO, seguirá fallando por limitación de plataforma. Metodología: parseo local del CSS compartido (132KB, 1642 reglas) + matching de selectores contra el DOM en vivo (obscura no persiste estado entre evaluate ni espera promesas; Playwright MCP caído ECONNREFUSED 9222). Resultado: usado en reposo ~30.5KB (23%), sin usar puro ~74.9KB (57%, sobre todo estilos base de componentes Webflow: w-file-upload, w-widget-twitter, w-webflow-badge, w-input + clases de otras páginas), media queries de otros viewports ~19.4KB (NO removibles), estados/interacción hover/w--open ~3.3KB (necesarios). Conclusión: Webflow sirve UN CSS site-wide minificado y no permite partirlo ni inline crítico fácilmente; tratar como "No aplica plataforma" (impacto ~300ms móvil). Única palanca real: borrar clases muertas desde el Designer auditando todas las páginas antes.
- [2026-08-25] Fila "Utiliza formatos de vídeo para contenidos animados" (efficient-animated-content) en https://www.nutec.com/: VERIFICADA SIN ERROR. 0 referencias .gif en el HTML publicado, 0 imágenes GIF en el DOM en vivo (35 imgs revisadas, ninguna gif ni background gif), 0 peticiones .gif en la traza de red; el contenido animado del hero ya es `<video>` con fuentes MP4/WebM. La fila debe salir verde / no aplicar.
- [2026-08-25] Fila "Preconecta orígenes" en https://www.nutec.com/ (home EN): VERIFICADA CORREGIDA. Trazas de red confirman que los únicos orígenes de terceros de la carga crítica son cdn.prod.website-files.com, fonts.googleapis.com, fonts.gstatic.com y d3e54v103j8qbb.cloudfront.net (jQuery), y TODOS tienen preconnect ANTES de su primer uso (website-files usa formato href-primero: `<link href rel="preconnect">`, por eso grep simple no lo veía). Los otros 13 preconnects cubren tags de GTM/pixel. Debe salir verde en PSI.
- [2026-08-25] Hallazgos menores en head del home EN (no afectan la fila): (1) ✅ RESUELTO por el usuario: preconnect/dns-prefetch de cdn.jsdelivr.net movidos ANTES del preload del CSS de Swiper (verificado en vivo: líneas 119-120 vs uso en 125). (2) Preload huérfano de Nunito v11 (`XRXV3I6Li01BKofINeaB.woff2`, línea 131) que ya no coincide con los archivos v32 que sirve Google Fonts → sugerencia nunca usada, borrar o actualizar. (3) PSI seguirá avisando ">4 preconnects" (hay 19 preconnects ahora): oportunidad de podar orígenes que quizá no se usan (adservice.google.com, cdnjs.cloudflare.com, analytics.google.com).
- [2026-08-25] Fila "texto visible durante carga de fuente" (font-display / FOIT): VERIFICADA CORREGIDA en https://www.nutec.com/. Las 10 @font-face del CSS de Webflow tienen `font-display:swap` (Untitled Sans Opt ×3, Founders Grotesk Opt ×4, Untitled Sans legacy ×3); Google Fonts Nunito carga con `display=swap` en URL; webflow-icons es data-URI base64 (no aplica). Debe salir verde en PSI.
- [2026-08-24] LCP de colchas investigado a fondo: la imagen Products_Blankets-Hero.avif está PERFECTA (13,382 bytes reales para 1920×1181, descargada en 44ms, prioridad High, preload activo isLinkPreload=true, loading=eager sin srcset necesario). El LCP "9,678ms" que muestra PSI es SIMULACIÓN de Lantern inflada por el trabajo de CPU del stack de tags (~700KB GTM + jQuery): el navegador REAL pintó el hero a los 1,401ms (verde). LCP observado vs simulado documentado como evidencia. Conclusión: nada que optimizar en el asset; única palanca real = reducir/diferir JS de marketing. Menor opcional: fetchpriority="high" en el img.
- [2026-08-24] Colchas `/es` (reporte PSI móvil correcto guardado por usuario): Performance **52**, FCP 2555ms, **LCP 9678ms (muy alto — revisar imagen del banner)**, TBT 684ms, SI 4127ms, CLS 0.005. "Preconecta orígenes" confirmada RESUELTA ("No additional origins are good candidates" + aviso >4 preconnects). "Utiliza formatos de vídeo para contenidos animados" (efficient-animated-content) NO aparece como falla y el HTML tiene 0 GIFs → no aplica/corregida.
- [2026-08-24] Fila "Preconecta orígenes" para página de producto (colchas): RESUELTA también aquí. Verificado estático en `/es/productos-e-ingenieria/colchas`: 17 preconnects en head todos ANTES de su primer uso; orígenes sin preconnect (tienda.nutec.com, tiendab2b, linkedin, youtube, jsdelivr) son solo enlaces de navegación o se preconectan dinámicamente vía script lite-youtube (que no se activa: 0 videos). PSI corrido por el usuario confirmó "No additional origins are good candidates" en ambos factores — PERO el reporte guardado en page-seed.html resultó ser de la página INGLESA `/products-engineering/blankets` (Desktop 84 / Móvil 73), no de colchas; el veredicto aplica igual por head compartido.
- [2026-08-24] PSI re-corrido (usuario guardó reporte en `page-seed.html`, análisis 16:47). Resultados MÓVIL: Performance **71**, FCP 2404ms, LCP 3616ms, SI 4893ms, TBT 509ms, CLS 0.022. ESCRITORIO: Performance 59 (TBT 4346ms lo hunde; JS de tags). Validado:
  - ✅ YA PASAN (auditorías ausentes del reporte): render-blocking-resources, lcp-lazy-loaded, prioritize-lcp-image, duplicate-modules-javascript.
  - ✅ "Preconecta orígenes": RESUELTA — PSI dice "No additional origins are good candidates for preconnecting". Aviso adicional: ">4 preconnect connections found… use sparingly" (hay ~17; oportunidad de podar, no de añadir).
  - ❌ "Enormes cargas útiles" SIGUE FALLANDO: total-byte-weight ≈ 12,972 KiB vs presupuesto 1,600 KiB. Causa: `startHeroVideo()` llama `v.play()` en window.load (+rIC timeout 3s) → el video entra COMPLETO en la traza de Lighthouse (~13MB webm y/o 8.4MB mp4 según run). FIX propuesto: arrancar el video solo con la PRIMERA INTERACCIÓN del usuario (pointerdown/touchstart/keydown), no en load.
  - Expectativa honesta: aun sin video quedan ~2–3MB (GTM first-party ~730KB en 4 archivos + gtag 184KB + fbevents 110KB + jquery 88KB + imágenes) → la fila podría seguir fallando sin recortar tags de marketing.
  - Menor: unused-css-rules persiste (31.8 KiB desperdiciados, 300ms móvil).
- [2026-08-24] SESIÓN COMPLETADA — fixes publicados y verificados en vivo:
  1. **Swiper duplicado eliminado** (TBT "módulos duplicados"): línea comentada en head de home `/es`; queda 1 sola carga vía loader condicional. Carruseles confirmados por el usuario en Chrome real.
  2. **"Enormes cargas útiles"**: video hero diferido (`preload="none"`, sin `autoplay`, arranque `startHeroVideo` tras window load + idle). Incidencia intermedia: al reemplazar el embed se perdió el listener `.is-ready` (video quedaba invisible con opacity:0); restaurado y consolidado en un solo script. Verificado en HTML publicado (v4).
  3. **Fuente legacy "Untitled Sans"** (.woff ×3, ~120KB): migrar a Opt las 5 clases que la usaban (description-author, title-about, heading-14, rich-text-block-3, filter-item); `--untitled` ya apuntaba a Opt. Usuario gestiona borrado de la fuente en Project Settings → Fonts.
  4. **"Minimiza CSS"**: verificado sin acción requerida — Webflow ya sirve minificado + brotli (24.7 KB transferidos).
- [2026-08-24] FIX aplicado y verificado: Swiper duplicado en home `/es`. El usuario comentó la línea `<script src="...swiper-bundle.min.js"></script>` del head (Page Settings). Verificado en vivo: 1 solo script de Swiper en DOM, `window.Swiper` definido vía loader condicional, CSS de Swiper aplicado (wrapper flex, hero 650px). Pendiente: confirmación visual de los 3 carruseles en Chrome real + re-corre PSI.
- [2026-08-24] Auditoría fresca de `/es` contra el Sheet de seguimiento (última columna 07-ago):
  - VERIFICADO corregido: compresión de texto (HTML gzip, CSS brotli), preconnect de cdn.jsdelivr.net ahora ANTES de su uso, preload del poster LCP en head, avif viejo comentado.
  - CONFIRMADO pendiente: `lang="en"` sigue en todas las páginas `/es` (SEO). Swiper duplicado SOLO en home `/es`: `swiper-bundle.min.js` en head (síncrono, línea ~134 del HTML) + loader condicional del body; el home EN solo lo carga 1 vez → borrar la línea del head de la página /es.
  - Video hero aún con `preload="auto"` + autoplay (1.78 MB compiten con LCP); existe listener que agrega `.is-ready` en loadeddata (fade-in). Propuesto inicio diferido post-load.
  - PSI API sin cuota hoy (429 anónima); validar filas "Carga diferida LCP" y "Precarga imagen LCP" corriendo PSI manualmente (los fixes se publicaron el 21-ago, después del chequeo del 07-ago).
  - Obscura no puede validar init de sliders (`e.children is not iterable` = limitación de su DOM sintético) ni medir el poster (no soporta shorthand `inset`); no tomar esos falsos positivos como reales.
- [2026-08-21] Se creó este archivo para mantener contexto persistente del proyecto y registrar el diagnóstico del home en español.
- [2026-08-21] Se detectaron errores persistentes en `/es`: `lang="en"`, enlaces internos sin prefijo `/es`, CTAs y textos en inglés, y schema/metadata global mezclado con contenido en español.
- [2026-08-21] Diagnóstico LCP (PSI: "Carga diferida de imagen LCP / Hay filas ocultas"): el LCP es el poster del video hero (`...6a32f9699b4a995ce7770d4f_Video%20Home%20-%20Nutec_poster.0000000.jpg`). El `<video id="213be94b-9cba-42de-f32f-ef399095d210-video">` NO tiene atributo nativo `poster` (solo `background-image` inline) y está oculto dentro de `div#car-home.swiper` (Swiper además falla: `TypeError: e.children is not iterable`). Hay además un preload innecesario de `Products_Blankets-Hero.avif`.
- [2026-08-21] Validado con obscura 0.2.0 instalado en `/tmp/obscura/obscura`: `videoPosterAttr=MISSING`, `videoInsideSwiperSlide=true`, `willLCPBeHidden=true`. 
- [2026-08-21] Nueva comprobación del HTML publicado: el video actual sigue usando `id="213be94b-9cba-42de-f32f-ef399095d210-video"`; el script agregado busca `213be94b-56db-234f-7df1-416aeb480242-video`, por lo que no encuentra el video. URLs confirmadas: MP4 `.../Video%20Home%20-%20Nutec_mp4.mp4` y WebM `.../Video%20Home%20-%20Nutec_webm.webm`.
- [2026-08-21] Para el aviso de precarga del LCP, la corrección será incluir el poster como `<img src>` visible en el HTML inicial del Embed, no depender únicamente de `poster`, `background-image` o CSS.
- [2026-08-21] Tras publicar, el HTML de `/es` ya contiene el `<img src>` del poster con `fetchpriority="high"`, `loading="eager"` y `decoding="async"`, además del video Embed con `poster` y fuentes MP4/WebM correctas. Obscura confirmó los atributos; el error de inicialización de Swiper (`e.children is not iterable`) todavía persiste.
- [2026-08-21] Revisión del inicializador de Swiper: la página carga `swiper@11` en `<head>` y vuelve a cargarlo dinámicamente en el footer. Se decidió conservar una sola carga y reemplazar el loader dinámico por una inicialización protegida; el error de `HTMLCollection` observado en obscura puede ser una limitación de ese navegador sintético.
- [2026-08-21] Auditoría de preconexiones: los orígenes críticos ya tienen `preconnect`, pero `cdn.jsdelivr.net` está declarado después de su preload/script de Swiper. Debe moverse antes de cualquier recurso de jsDelivr; no se deben añadir más orígenes sin confirmar recursos reales en Network/GTM.

## Decisiones importantes
- [2026-08-25] El usuario aplicó el arranque diferido del video (load + idle) también en el home EN, replicando /es: en la práctica se sustituye el autoplay estricto por reproducción casi inmediata tras load (matiza la decisión del 08-24; el requisito visual del cliente se mantiene vía fade-in rápido).
- [2026-08-24] VIDEO HERO — REQUISITO DEL CLIENTE/MARKETING: el video DEBE reproducirse desde el inicio (autoplay). Se descarta el arranque por primera interacción. Consecuencia aceptada: la fila "Enormes cargas útiles" no podrá ponerse verde (video + ~1MB de tags > presupuesto 1,600 KiB). Palancas acordadas dentro del requisito: dejar UNA sola fuente (mp4) y recomprimirla (~2.5–3.5MB objetivo); marca la fila como limitación aceptada tipo "No aplica".
- [2026-08-24] TIPOGRAFÍAS — análisis cerrado sin acción adicional (decisión del usuario):
  - "Untitled Sans Opt" ES una fuente principal del sitio: texto base (.body vía var(--untitled)) y menú (.nav.head-menu) en todas las páginas. NO se elimina.
  - Quedan ~5 clases en la legacy "Untitled Sans" (.sub-wrapper, .sub-wrapper-landing, .description-author, .rich-text-block-3, .filter-item) + sus 3 @font-face .woff (~120KB). Migración pendiente si se retoma.
  - HALLAZGO PARA EL FUTURO: 30 reglas piden `font-family: Founders Grotesk` (sin -Opt ni fallback) pero esa familia NO existe en @font-face → esos textos caen al serif default del navegador salvo que el visitante tenga la fuente instalada localmente. Afecta entre otras a .heading.blue, .feed-card-title, .dd.menu-item, .rich-text.take h2, .ul.font-head. FIX cuando el cliente lo reporte: cambiar esas reglas a "Founders Grotesk Opt" en el Designer. El usuario decide no tocarlo ahora porque la tipografía sí aparece usada en otras páginas y el cliente nota los cambios de tipografía.
- Registrar hallazgos del análisis del home `/es` antes de proponer cambios para poder rastrear causas repetidas.
- El patrón observado sugiere problemas de configuración/origen en CMS o plantilla multilenguaje, no solo errores aislados de copy.
- El Background Video nativo de Webflow no permite elegir poster en UI (genera `data-poster-url` automático del frame 0); para el atributo nativo `poster` hay que usar Custom Code o reemplazar por Embed HTML.

## Archivos clave
- `READYS.md`: memoria persistente del contexto del proyecto.
- `home-eng.html`: referencia local de la versión inglesa/exportada, útil para comparar componentes globales.
- `es/productos-e-ingenieria/colchas.html`: ejemplo local de una página en español con enlaces/localización mejor resueltos.

## Pendientes / Siguientes pasos
- Aplicar fix definitivo del video hero: sustituir arranque por load/idle por arranque en primera interacción (snippet ya propuesto al usuario); re-publicar y re-corre PSI.
- Actualizar Sheet con resultados del 24-ago (texto sugerido entregado): Preconecta = resuelta; Enormes cargas útiles = sigue, causa video+tags.
- Decisión pendiente del equipo de marketing: GTM first-party (~730KB) + fbevents + gtag dominan el peso/TBT restante.
- Confirmar que la fuente legacy "Untitled Sans" fue eliminada de Project Settings → Fonts tras migrar las 5 clases. [2026-08-25: las 3 @font-face .woff legacy SIGUEN publicadas en el CSS compartido, ya con font-display:swap; el borrado en Project Settings no se ha hecho o no se ha republicado.]
- [APARCADO] Fix `lang="en"`→`es` en las 165 páginas `/es`: NO viene en el Sheet del cliente, el usuario decidió dejarlo fuera por ahora. Snippet site-wide ya propuesto (Site Settings → Head) por si se retoma.
- Revisar en el CMS/Webflow qué componentes del home `/es` están heredando campos globales en inglés (CTA/link HTP, card MaxBoard 2600 FP, ruta de búsqueda, textos mezclados).
- Filas del Sheet marcadas "No aplica servidor" (JS Webflow, compresión ya activa, DOM excesivo Bajo): documentadas como limitaciones de plataforma.
